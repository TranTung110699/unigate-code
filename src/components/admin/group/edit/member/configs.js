const formNames = {
  SEARCH_FORM_WHEN_ADD_MEMBERS: 'abc',
  resultFormName: 'user_in_group_search_result',
};

export const expandFields = ['user_organizations', 'positions', 'phongbans'];

export default formNames;
