import React, { Component } from 'react';
import Icon from 'antd/lib/icon';

class HelpIcon extends Component {
  render() {
    return <Icon type="question-circle" style={{ color: 'rgba(0,0,0,.45)' }} />;
  }
}

export default HelpIcon;
