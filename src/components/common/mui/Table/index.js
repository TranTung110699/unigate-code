import {
  Table as xTable,
  TableBody as xTableBody,
  TableHeader as xTableHeader,
  TableHeaderColumn as xTableHeaderColumn,
  TableRow as xTableRow,
} from 'material-ui/Table';

import xTableRowColumn from './TableRowColumn';

export const Table = xTable;
export const TableBody = xTableBody;
export const TableHeader = xTableHeader;
export const TableHeaderColumn = xTableHeaderColumn;
export const TableRow = xTableRow;
export const TableRowColumn = xTableRowColumn;
